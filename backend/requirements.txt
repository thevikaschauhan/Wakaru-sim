# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= 核心框架 =============
flask>=3.0.0
flask-cors>=6.0.0
sentry-sdk[flask]>=2.0.0

# ============= LLM 相关 =============
# OpenAI SDK（统一使用 OpenAI 格式调用 LLM）
openai>=1.0.0

# ============= Zep Cloud =============
zep-cloud==3.13.0

# ============= OASIS 社交媒体模拟 =============
# OASIS 社交模拟框架
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= 工具库 =============
# 环境变量加载
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0

# ============= 生产服务器 =============
gunicorn>=22.0,<23

# ============= 异步任务队列 (issue #20) =============
# Redis + RQ move /api/cart-recovery/jobs off the synchronous request path.
# Installed in the deployed image (this is the prod install path; see Dockerfile);
# the worker runs as a SEPARATE Railway service off the same image.
redis>=5.0,<6
rq>=1.16,<2

# ============= Rate limiting (issue #12) =============
# Per-IP rate limit on the paid cart-recovery endpoints. Redis storage so the
# limit coheres across the 2 gunicorn workers (in-process state would not).
flask-limiter>=3.5,<4
